/**
 * Stub module for @revolist/revogrid-pro
 * 
 * This stub provides type-compatible exports to prevent build errors
 * when revogrid-pro is not installed. The actual revogrid functionality
 * is provided by @revolist/revogrid package.
 */

// Re-export everything from the base revogrid package if available
// Otherwise provide empty stubs
export const RevoGridPro = {};
export default RevoGridPro;

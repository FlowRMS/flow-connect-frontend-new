/**
 * CommissionsEmptyState Component
 * Displayed when no checks match the current filters
 */

export function CommissionsEmptyState() {
  return (
    <div className="px-6 py-12 text-center text-[var(--muted-foreground)]">
      No checks found
    </div>
  );
}


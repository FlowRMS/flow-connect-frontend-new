/**
 * Mock Task Data
 * This file is kept for reference but the app now uses real API data
 * @deprecated Use API data from useCRMTasks hook instead
 */

import type { Task } from './types';

// Empty array - tasks are now fetched from the API
export const MOCK_TASKS: Task[] = [];

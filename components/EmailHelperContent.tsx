/**
 * Email Helper Content - Campaigns & Rules
 * Wrapper component that imports the modular implementation
 */

'use client';

import CampaignsContent from './campaigns/CampaignsContent';

export default function EmailHelperContent() {
  return <CampaignsContent />;
}
